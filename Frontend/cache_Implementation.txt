Implementation for caching filtered cardata:
•	Save the cached data as a string of all the ids with a connector like : “מ”  so we could take it and do .split(“מ”) (we would use Hebrew because there is 0 chance of anther hebrew latter in the id) and get an array of all the ids of the filtered cardatas and then just do ctx.cardats.filter(car => cached_data.includes(car._id)) so we will get all the cardatas that have been filtered in minimal time without overloading data on our redis db and without needing to update its entries every update that cardatas has (except some  specific use cases)
•	Same as up there by saving as an array to avoid the connector stage, resulting in heavier data being saved on the redis might effect performance
 can add to performance if we set cardats in mongo a new index valued as its id (to be checked how mongo calculates latters and symbols) and then we will call cardatas in redis (redis can preforme function calls) and read them by there index when needed ( role 1 to 5, etc... [will be banchmarked for other implementations in case its faster then fetching data from mongo with regular filters])